{{- if .Values.strategyBuilderApi.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tradestream.fullname" . }}-strategy-builder-api
  labels:
    {{- include "tradestream.labels" . | nindent 4 }}
    app.kubernetes.io/component: strategy-builder-api
spec:
  replicas: {{ .Values.strategyBuilderApi.replicaCount | default 1 }}
  selector:
    matchLabels:
      {{- include "tradestream.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: strategy-builder-api
  template:
    metadata:
      labels:
        {{- include "tradestream.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: strategy-builder-api
    spec:
      containers:
        - name: strategy-builder-api
          image: "{{ .Values.strategyBuilderApi.image.repository }}:{{ .Values.strategyBuilderApi.image.tag }}"
          imagePullPolicy: {{ .Values.strategyBuilderApi.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - name: http
              containerPort: {{ .Values.strategyBuilderApi.service.port | default 8095 }}
              protocol: TCP
          env:
            - name: POSTGRES_HOST
              value: "{{ include "tradestream.fullname" . }}-postgresql"
            - name: POSTGRES_PORT
              value: "5432"
            - name: POSTGRES_DATABASE
              value: "tradestream"
            - name: POSTGRES_USERNAME
              value: "postgres"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ tpl .Values.strategyBuilderApi.database.passwordSecret.name . }}
                  key: {{ .Values.strategyBuilderApi.database.passwordSecret.key }}
            - name: API_PORT
              value: "{{ .Values.strategyBuilderApi.service.port | default 8095 }}"
            - name: API_HOST
              value: "{{ .Values.strategyBuilderApi.service.host | default "0.0.0.0" }}"
          resources:
            {{- toYaml .Values.strategyBuilderApi.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tradestream.fullname" . }}-strategy-builder-api
  labels:
    {{- include "tradestream.labels" . | nindent 4 }}
    app.kubernetes.io/component: strategy-builder-api
spec:
  type: {{ .Values.strategyBuilderApi.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.strategyBuilderApi.service.port | default 8095 }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tradestream.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: strategy-builder-api
{{- end }}
