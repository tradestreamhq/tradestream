{{- if .Values.signalQuality.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tradestream.fullname" . }}-signal-quality
  labels:
    {{- include "tradestream.labels" . | nindent 4 }}
    app.kubernetes.io/component: signal-quality
spec:
  replicas: {{ .Values.signalQuality.replicaCount | default 1 }}
  selector:
    matchLabels:
      {{- include "tradestream.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: signal-quality
  template:
    metadata:
      labels:
        {{- include "tradestream.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: signal-quality
    spec:
      containers:
        - name: signal-quality
          image: "{{ .Values.signalQuality.image.repository }}:{{ .Values.signalQuality.image.tag }}"
          imagePullPolicy: {{ .Values.signalQuality.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - name: http
              containerPort: {{ .Values.signalQuality.service.port | default 8090 }}
              protocol: TCP
          env:
            - name: POSTGRES_HOST
              value: "{{ include "tradestream.fullname" . }}-postgresql"
            - name: POSTGRES_PORT
              value: "5432"
            - name: POSTGRES_DATABASE
              value: "tradestream"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "{{ include "tradestream.fullname" . }}-postgresql"
                  key: "postgres-password"
            - name: PORT
              value: "{{ .Values.signalQuality.service.port | default 8090 }}"
          resources:
            {{- toYaml .Values.signalQuality.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tradestream.fullname" . }}-signal-quality
  labels:
    {{- include "tradestream.labels" . | nindent 4 }}
    app.kubernetes.io/component: signal-quality
spec:
  type: {{ .Values.signalQuality.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.signalQuality.service.port | default 8090 }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tradestream.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: signal-quality
{{- end }}
