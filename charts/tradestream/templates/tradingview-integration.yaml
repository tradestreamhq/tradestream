{{- if .Values.tradingviewIntegration.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tradestream.fullname" . }}-tradingview-integration
  labels:
    {{- include "tradestream.labels" . | nindent 4 }}
    app.kubernetes.io/component: tradingview-integration
spec:
  replicas: {{ .Values.tradingviewIntegration.replicaCount | default 1 }}
  selector:
    matchLabels:
      {{- include "tradestream.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: tradingview-integration
  template:
    metadata:
      labels:
        {{- include "tradestream.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: tradingview-integration
    spec:
      containers:
        - name: tradingview-integration
          image: "{{ .Values.tradingviewIntegration.image.repository }}:{{ .Values.tradingviewIntegration.image.tag }}"
          imagePullPolicy: {{ .Values.tradingviewIntegration.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - name: http
              containerPort: {{ .Values.tradingviewIntegration.service.port | default 8090 }}
              protocol: TCP
          env:
            - name: POSTGRES_HOST
              value: "{{ include "tradestream.fullname" . }}-postgresql"
            - name: POSTGRES_PORT
              value: "5432"
            - name: POSTGRES_DATABASE
              value: "tradestream"
            - name: POSTGRES_USERNAME
              value: "postgres"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "{{ include "tradestream.fullname" . }}-postgresql"
                  key: "postgres-password"
            - name: API_PORT
              value: "{{ .Values.tradingviewIntegration.service.port | default 8090 }}"
            - name: API_HOST
              value: "{{ .Values.tradingviewIntegration.service.host | default "0.0.0.0" }}"
          resources:
            {{- toYaml .Values.tradingviewIntegration.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tradestream.fullname" . }}-tradingview-integration
  labels:
    {{- include "tradestream.labels" . | nindent 4 }}
    app.kubernetes.io/component: tradingview-integration
spec:
  type: {{ .Values.tradingviewIntegration.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.tradingviewIntegration.service.port | default 8090 }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tradestream.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: tradingview-integration
{{- end }}
