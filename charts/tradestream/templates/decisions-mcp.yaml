{{- if .Values.decisionsMcp.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tradestream.fullname" . }}-decisions-mcp
  labels:
    {{- include "tradestream.labels" . | nindent 4 }}
    app.kubernetes.io/component: decisions-mcp
spec:
  replicas: {{ .Values.decisionsMcp.replicaCount | default 1 }}
  selector:
    matchLabels:
      {{- include "tradestream.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: decisions-mcp
  template:
    metadata:
      labels:
        {{- include "tradestream.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: decisions-mcp
    spec:
      containers:
        - name: decisions-mcp
          image: "{{ .Values.decisionsMcp.image.repository }}:{{ .Values.decisionsMcp.image.tag }}"
          imagePullPolicy: {{ .Values.decisionsMcp.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - name: http
              containerPort: {{ .Values.decisionsMcp.service.port | default 8080 }}
              protocol: TCP
          env:
            - name: POSTGRES_HOST
              value: {{ tpl .Values.decisionsMcp.database.host . | quote }}
            - name: POSTGRES_PORT
              value: {{ .Values.decisionsMcp.database.port | quote }}
            - name: POSTGRES_DATABASE
              value: "{{ .Values.decisionsMcp.database.database }}"
            - name: POSTGRES_USERNAME
              value: "{{ .Values.decisionsMcp.database.username }}"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ tpl .Values.decisionsMcp.database.passwordSecret.name . }}
                  key: {{ .Values.decisionsMcp.database.passwordSecret.key }}
            - name: MCP_TRANSPORT
              value: "{{ .Values.decisionsMcp.config.mcpTransport }}"
            - name: MCP_PORT
              value: {{ .Values.decisionsMcp.service.port | quote }}
          args:
            - "--postgres_host=$(POSTGRES_HOST)"
            - "--postgres_port=$(POSTGRES_PORT)"
            - "--postgres_database=$(POSTGRES_DATABASE)"
            - "--postgres_username=$(POSTGRES_USERNAME)"
            - "--postgres_password=$(POSTGRES_PASSWORD)"
            - "--mcp_transport=$(MCP_TRANSPORT)"
            - "--mcp_port=$(MCP_PORT)"
          resources:
            {{- toYaml .Values.decisionsMcp.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tradestream.fullname" . }}-decisions-mcp
  labels:
    {{- include "tradestream.labels" . | nindent 4 }}
    app.kubernetes.io/component: decisions-mcp
spec:
  type: {{ .Values.decisionsMcp.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.decisionsMcp.service.port | default 8080 }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tradestream.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: decisions-mcp
{{- end }}
