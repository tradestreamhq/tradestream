{{- if .Values.paperTrading.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tradestream.fullname" . }}-paper-trading
  labels:
    {{- include "tradestream.labels" . | nindent 4 }}
    app.kubernetes.io/component: paper-trading
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "tradestream.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: paper-trading
  template:
    metadata:
      labels:
        {{- include "tradestream.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: paper-trading
    spec:
      containers:
        - name: paper-trading
          image: "{{ .Values.paperTrading.image.repository }}:{{ .Values.paperTrading.image.tag }}"
          imagePullPolicy: {{ .Values.paperTrading.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - name: http
              containerPort: {{ .Values.paperTrading.config.port }}
              protocol: TCP
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.paperTrading.secrets.postgresPassword.name }}
                  key: {{ .Values.paperTrading.secrets.postgresPassword.key }}
            - name: MCP_MARKET_URL
              value: "http://{{ include "tradestream.fullname" . }}-market-mcp:8080"
            - name: MCP_SIGNAL_URL
              value: "http://{{ include "tradestream.fullname" . }}-signal-mcp:8080"
          args:
            - "--postgres_host={{ .Values.paperTrading.config.postgresHost }}"
            - "--postgres_port={{ .Values.paperTrading.config.postgresPort }}"
            - "--postgres_database={{ .Values.paperTrading.config.postgresDatabase }}"
            - "--postgres_username={{ .Values.paperTrading.config.postgresUsername }}"
            - "--postgres_password=$(POSTGRES_PASSWORD)"
            - "--mcp_market_url=$(MCP_MARKET_URL)"
            - "--mcp_signal_url=$(MCP_SIGNAL_URL)"
            - "--port={{ .Values.paperTrading.config.port }}"
            - "--host=0.0.0.0"
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.paperTrading.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tradestream.fullname" . }}-paper-trading
  labels:
    {{- include "tradestream.labels" . | nindent 4 }}
    app.kubernetes.io/component: paper-trading
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.paperTrading.config.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tradestream.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: paper-trading
{{- end }}
