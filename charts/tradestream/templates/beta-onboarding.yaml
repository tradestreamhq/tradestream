{{- if .Values.betaOnboarding.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tradestream.fullname" . }}-beta-onboarding
  labels:
    {{- include "tradestream.labels" . | nindent 4 }}
    app.kubernetes.io/component: beta-onboarding
spec:
  replicas: {{ .Values.betaOnboarding.replicaCount | default 1 }}
  selector:
    matchLabels:
      {{- include "tradestream.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: beta-onboarding
  template:
    metadata:
      labels:
        {{- include "tradestream.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: beta-onboarding
    spec:
      containers:
        - name: beta-onboarding
          image: "{{ .Values.betaOnboarding.image.repository }}:{{ .Values.betaOnboarding.image.tag }}"
          imagePullPolicy: {{ .Values.betaOnboarding.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - name: http
              containerPort: {{ .Values.betaOnboarding.service.port | default 8091 }}
              protocol: TCP
          env:
            - name: POSTGRES_HOST
              value: "{{ include "tradestream.fullname" . }}-postgresql"
            - name: POSTGRES_PORT
              value: "5432"
            - name: POSTGRES_DATABASE
              value: "tradestream"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ tpl .Values.betaOnboarding.database.passwordSecret.name . }}
                  key: {{ .Values.betaOnboarding.database.passwordSecret.key }}
            - name: PORT
              value: "{{ .Values.betaOnboarding.service.port | default 8091 }}"
          resources:
            {{- toYaml .Values.betaOnboarding.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tradestream.fullname" . }}-beta-onboarding
  labels:
    {{- include "tradestream.labels" . | nindent 4 }}
    app.kubernetes.io/component: beta-onboarding
spec:
  type: {{ .Values.betaOnboarding.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.betaOnboarding.service.port | default 8091 }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tradestream.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: beta-onboarding
{{- end }}
