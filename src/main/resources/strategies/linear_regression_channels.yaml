name: LINEAR_REGRESSION_CHANNELS
description: Price breaks linear regression channel boundaries
complexity: MODERATE
parameterMessageType: com.verlumen.tradestream.strategies.LinearRegressionChannelsParameters

indicators:
  - id: closePrice
    type: CLOSE
    input: close
    params: {}
  - id: upperBand
    type: BOLLINGER_UPPER
    input: close
    params:
      period: "${period}"
      multiplier: "${multiplier}"
  - id: lowerBand
    type: BOLLINGER_LOWER
    input: close
    params:
      period: "${period}"
      multiplier: "${multiplier}"

entryConditions:
  - type: CROSSED_UP
    indicator: closePrice
    params:
      crosses: upperBand

exitConditions:
  - type: CROSSED_DOWN
    indicator: closePrice
    params:
      crosses: lowerBand

parameters:
  - name: period
    type: INTEGER
    min: 10
    max: 50
    defaultValue: 20
  - name: multiplier
    type: DOUBLE
    min: 1.0
    max: 3.0
    defaultValue: 2.0
